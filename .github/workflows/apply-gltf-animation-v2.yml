name: Apply glTF Animation V2

on:
  push:
    branches:
      - feature/gltf-animation-v2

permissions:
  contents: write

jobs:
  patch:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply glTF scene v2 data model
        run: python3 .github/scripts/apply_gltf_animation_v2.py
      - name: Commit generated source changes
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Assets: add glTF scene skinning and animation artifact v2"
          git push
